package com.oops.day6;

import java.util.Scanner;

public class Driver {
    public static void main(String[] args) {
        /*Calculate calculator = new Calculate();
        calculator.display(); // Calls the method without parameters
        calculator.display(5);
        calculator.display(6.9);// Calls the method with an integer parameter
         */

        /*
        Parent parent = new Parent();
        Child child = new Child();
        Parent objectref;
        objectref = parent; // Parent reference can point to Parent object
        objectref.display1();
        objectref = child;
        objectref.display1();
        Scanner input = new Scanner(System.in);
        System.out.println("Enter a number for 1)parent 2) child :");
        int choice = input.nextInt();
        if (choice == 1) {
            objectref = parent; // Parent reference can point to Parent object
        } else {
            objectref = child; // Parent reference can point to Child object
        }
        objectref.display1();
        */
        /*
        DiscountCalculator discountCalculator = new DiscountCalculator();
        System.out.println("baseed on discount percentage price 100, discount 10 " + discountCalculator.calculateDiscount(1000, 10));
        System.out.println("based  on flat discount , price 500 discountamount 100 flat is true " +  discountCalculator.calculateDiscount(500,100,true));
        System.out.println("based on flat normal discount "+ discountCalculator.calculateDiscount(1000));
        // Example usage of the method with price and discount percent
        */


    }
}
